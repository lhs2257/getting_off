# Getting Off - 직장인 출퇴근 교통 알림 앱

> 이 파일은 Claude Code가 이 프로젝트에서 반드시 따라야 하는 지침을 정의합니다.
> 프로젝트 상세 정보는 `PROJECT.md`, 작업 현황은 `PROGRESS.md`를 참조합니다.

---

## [프로젝트] 기본 정보

- **프로젝트명**: Getting Off (직장인 출퇴근 교통 알림 앱)
- **기술 스택**: React Native + Expo SDK 57 + TypeScript
- **아키텍처**: FSD(Feature-Sliced Design) + Clean Architecture
- **앱 소스 경로**: `src/` (Expo 프로젝트 루트)
- **리서치 자료**: `research/` 폴더 참조

---

## [철학] 핵심 운영 원칙

### 원칙 1: 일 쪼개기 (Task Decomposition)

- 모든 업무를 단계별로 분해하여 수행합니다
- 업무를 항상 여러 단계로 분해하고, PROGRESS.md에 기록
- 각 단계는 독립적이고 검증 가능한 단위
- **각 단계 완료 후 사용자 승인** 받고 다음 단계 진행

### 원칙 2: 문서화 필수 (Documentation First)

- 어떤 작업이 완료되면 **반드시** 관련 문서를 최신화
- PROGRESS.md: 전체 진행 현황 업데이트
- 해당 모듈의 README.md: 변경사항 반영
- 문서와 실제 코드/상태는 항상 일치해야 합니다

### 원칙 3: 모듈화 (Modularization)

- 기능 단위로 독립적 모듈화
- 각 모듈은 자체 폴더, README, 테스트 보유
- 모듈 간 직접 의존성 최소화
- FSD 레이어 규칙: 위에서 아래로만 참조 (app > pages > widgets > features > entities > shared)

### 원칙 4: 문서 항상 최신화 (Living Documentation)

- 코드 변경 시 문서 동시 업데이트 (같은 커밋)
- PROGRESS.md에 현재 위치, 다음 할 일이 항상 명시
- 누가 이어받더라도 현황을 즉시 파악 가능

### 원칙 5: 엄격한 네이밍 규칙

- **컴포넌트**: PascalCase (`RouteCard.tsx`)
- **훅**: camelCase + use (`useCommuteSession.ts`)
- **폴더**: kebab-case (`commute-session/`)
- **상수**: UPPER_SNAKE_CASE (`MAX_GEOFENCE_COUNT`)
- **타입**: PascalCase + 접미사 (`RouteEntity`, `TransitApiResponse`)
- **문서**: `PROGRESS.md`, `PROJECT.md`, `research/[NN]-[topic]/`, `[module]/README.md`

---

## [지침] 레퍼런스 및 외부 자료 관리 (강제)

사용자로부터 참조 파일이 필요한 상황이 발생하면, 구체적 경로를 먼저 생성하고 안내해야 합니다.

```
references/
  01-design/          # 디자인 시안, 와이어프레임
  02-api-docs/        # API 문서, 스키마
  03-existing-code/   # 기존 코드 참조
  04-data-samples/    # 샘플 데이터
  05-style-guides/    # 문서 스타일 레퍼런스
```

- 프로젝트 유형에 따라 필요한 하위 폴더만 생성합니다
- 레퍼런스 파일은 수정하지 않음 (읽기 전용 취급)

---

## [지침] 문서 작성 품질 기준 (강제)

### 서식 규칙

- 헤더 깊이: H1은 문서 제목 전용, 본문은 H2~H3만 사용합니다
- 불릿 목록: 하이픈(`-`) 통일 사용
- 코드 블록: 반드시 언어 지정
- 구분선(`---`): 대주제 전환 시에만 사용합니다

### 한국어 기술 문서 스타일

- 격식체(합쇼체)만 사용합니다: "~합니다", "~입니다"
- 영문 기술 용어 병기: "모듈화(Modularization)"
- 영문 용어 앞뒤 띄어쓰기 유지

---

## [작업 흐름] 매 작업 시 따라야 할 절차

### 작업 시작 시

1. PROGRESS.md에서 현재 진행 상태 확인
2. `git log --oneline -10`으로 최근 커밋 이력 확인
3. `git status`로 커밋되지 않은 변경사항 확인
4. 다음 수행할 작업 확인
5. 작업 시작 전 관련 파일/모듈 읽기
6. 사용자에게 작업 시작 알림

### 작업 진행 중

1. 하나의 작업에만 집중합니다
2. 모듈 경계를 넘지 않는 변경
3. 변경 사항이 다른 모듈에 영향 주는지 확인

### 작업 완료 시 (매 턴 종료 전 필수)

1. 해당 모듈의 README.md 업데이트
2. PROGRESS.md 업데이트
3. 사용자에게 완료 보고 및 다음 단계 확인
4. 사용자 승인 후 다음 작업 진행

### 에러 발생 시 복구 절차

- **코드 실수**: 즉시 중단 -> `git diff` 확인 -> 복구 -> PROGRESS.md 기록
- **잘못된 방향**: 체크포인트 커밋 -> 대안 제시 -> 사용자 선택
- **복구 불가**: `git log`로 정상 커밋 찾아 reset 제안 (사용자 확인 필수)
- **외부 오류**: 3회 재시도 -> 우회 탐색 -> 해결 불가 시 보고

---

## [참고] 리서치 폴더 활용

`research/` 폴더에는 프로젝트 초기화 시 수행한 리서치 결과가 저장되어 있습니다:

- `01-methodology/` - FSD + Clean Architecture 방법론
- `02-folder-structure/` - 프로젝트 폴더 구조
- `03-best-practices/` - 기술 스택, 공공 API 정보
- `04-naming-conventions/` - 네이밍 규칙, 도메인 용어 대응표

---

## [보존] Compact Instructions

컨텍스트 압축 시 반드시 보존해야 할 항목:
- 현재 진행 중인 작업과 단계 (PROGRESS.md 참조)
- 수정된 파일 전체 목록
- 사용자가 내린 결정 사항과 승인 내역
- 다음 수행할 작업과 우선순위
- 프로젝트의 모듈 구조와 현재 작업 중인 모듈 경로

---

## [언어] 커뮤니케이션 및 인코딩 설정

### 언어 설정

- 사용자와의 대화: **한국어**
- 코드 내 변수/함수명: **영어** (camelCase/PascalCase)
- 문서 작성: **한국어**
- 커밋 메시지: **한국어**

### 커밋 메시지 형식 (강제)

형식: `<타입>(<범위>): <요약>` 또는 `<타입>: <요약>`

| 타입 | 의미 |
|------|------|
| 기능 | 새로운 기능 추가 |
| 수정 | 버그 수정 |
| 문서 | 문서 변경 |
| 구조 | 리팩토링 |
| 설정 | 설정/환경 변경 |
| 초기화 | 프로젝트 초기 세팅 |

### 이모지 사용 금지 (강제)

- 모든 영역에서 이모지 사용 금지
- 상태 표시: `[완료]`/`[v]`, `[진행중]`/`[>]`, `[대기]`/`[ ]`, `[주의]`/`[!]`, `[중요]`/`[*]`

### 한글 인코딩 규칙 (강제)

- 모든 파일은 **UTF-8 (BOM 없음)**
- 한글 경로는 반드시 큰따옴표로 감싸기
- 셸 명령 실행 전 필요 시 `chcp 65001`

---

## [검증] 매 응답 전 자기 점검 (이 섹션은 반드시 CLAUDE.md 최하단에 위치)

응답을 완료하기 전에 다음을 확인합니다:
- 파일을 수정했다면, PROGRESS.md와 관련 README.md도 업데이트했는가?
- 작업이 완료되었다면, 사용자에게 다음 단계 승인을 요청했는가?
- 현재 작업 모듈 외의 파일을 수정하지 않았는가?
- 이모지를 사용하지 않고 텍스트 마커를 사용했는가?
- 새 파일 생성 시, 기존 파일 수정으로 불가능한지 먼저 확인했는가?
- 문서에 격식체(합쇼체)를 사용했는가?
- 한글 경로를 큰따옴표로 감쌌는가?
- 커밋 메시지가 `<타입>(<범위>): <요약>` 형식을 따르는가?
- 커밋되지 않은 변경사항이 남아있지 않은가?
